/*
// Element Getters
const links = document.querySelectorAll('.link-icon');

// Hover Event Listeners:
links.forEach(link=>{
  link.addEventListener("mouseover", function(event){
    link.classList.remove('material-icons-outlined');
    link.classList.add('material-icons-two-tone')
  })
})
*/
